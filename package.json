{
  "name": "network-control",
  "version": "0.0.1",
  "description": "Server app for controlling network devices",
  "author": "Radosław Drab",
  "license": "ISC",
  "main": "dist/index.cjs",
  "type": "module",
  "scripts": {
    "start": "dotenvx run -f .env.dev -- tsx --watch src/index.ts",
    "prebuild": "shx rm -rf dist/ && npm run type-check",
    "build": "dotenvx run -f .env.prod -- node esbuild.mjs",
    "postbuild": "npm run build-exe && shx rm sea-prep.blob",
    "build-exe": "node --experimental-sea-config sea-config.json && node -e \"require('fs').copyFileSync(process.execPath, 'dist/index.exe')\" && postject dist/index.exe NODE_SEA_BLOB sea-prep.blob --sentinel-fuse NODE_SEA_FUSE_fce680ab2cc467b6e072b8b5df1996b2",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@dotenvx/dotenvx": "^1.6.4",
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "concurrently": "^8.2.2",
    "esbuild": "^0.23.0",
    "express": "^4.19.2",
    "postject": "^1.0.0-alpha.6",
    "shx": "^0.3.4",
    "tsx": "^4.16.2",
    "typescript": "^5.5.4"
  }
}
